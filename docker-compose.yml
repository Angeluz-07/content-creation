services:
  mongodb:
    image: mongo:7.0.8
    container_name: mongo_cc
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASS}
    volumes:
      - ./.mongo_data:/data/db
    command: ["--wiredTigerCacheSizeGB", "0.25"] # Límite interno de Mongo para cache
    deploy:
      resources:
        limits:
          cpus: "0.50" # Máximo 50% de un núcleo de CPU
          memory: 512M # Máximo 512MB de RAM
        reservations:
          memory: 128M # Garantiza al menos 128MB
  compass:
    image: haohanyang/compass-web:latest
    container_name: compass_web_panel
    restart: always
    ports:
      - "8080:8080"
    environment:
      # Conecta automáticamente al servicio de mongo de arriba
      - CW_MONGO_URI=mongodb://${MONGO_USER}:${MONGO_PASS}@mongodb:27017
      - CW_ENABLE_EDIT_CONNECTIONS=true
    depends_on:
      - mongodb
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
  redis:
    image: redis:7.2-alpine
    container_name: redis_cc
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
  qdrant:
    image: qdrant/qdrant:v1.17.1
    container_name: qdrant_vector_db
    restart: always
    ports:
      - "6333:6333" # API REST y Web Dashboard
      - "6334:6334" # gRPC (por si lo necesitas más adelante)
    environment:
      - QDRANT__SERVICE__ENABLE_PROFILING=true
    volumes:
      - ./.qdrant_data:/qdrant/storage:z
    deploy:
      resources:
        limits:
          cpus: "0.50" # Máximo 50% de un núcleo de CPU
          memory: 512M # Máximo 512MB de RAM (ideal para tus colecciones de testing)
        reservations:
          memory: 128M # Garantiza al menos 128MB
  # CUSTOM SERVICES
  embedding:
    image: angeluz07/embedding:0.0.1 # use local if exists
    build: # if doesnt, build it
      context: .
      dockerfile: ./services/embedding/Dockerfile
    container_name: embedding
    ports:
      - "8001:8001"
    environment:
      - UVICORN_RELOAD=${UVICORN_RELOAD:-false}
    volumes:
      - ./services/embedding/.data:/app/.data:z
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1536M
        reservations:
          memory: 256M
  # neo4j:
  #   image: neo4j:5.18.0-community
  #   container_name: neo4j_graph_db
  #   restart: always
  #   ports:
  #     - "7474:7474"       # HTTP (Dashboard/Browser de Neo4j)
  #     - "7687:7687"       # Bolt (Protocolo para conectar desde Python/Node)
  #   environment:
  #     - NEO4J_AUTH=${NEO4J_USER}/${NEO4J_PASS}
  #     # Optimización de memoria interna para que quepa en los 512MB de Docker
  #     - NEO4J_server_memory_heap_initial__size=128m
  #     - NEO4J_server_memory_heap_max__size=256m
  #     - NEO4J_server_memory_pagecache_size=128m
  #   volumes:
  #     - ./.neo4j_data/data:/data
  #     - ./.neo4j_data/logs:/logs
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '0.50'     # Máximo 50% de un núcleo de CPU
  #         memory: 512M    # Máximo 512MB de RAM
  #       reservations:
  #         memory: 128M    # Garantiza al menos 128MB
